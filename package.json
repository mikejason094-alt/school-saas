{
  "name": "school-saas",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "node server/server.js"
  }
}
